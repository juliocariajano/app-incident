_schema-version: 3.3.0
ID: app-incidents
version: 1.0.0
description: "A CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: app-incidents-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      memory: 256M
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: app-incidents-auth
      - name: app-incidents-db
      - name: app-incidents-destination

  - name: app-incidents-db-deployer
    type: hdb
    path: gen/db
    parameters:
      memory: 256M
      buildpack: nodejs_buildpack
    requires:
      - name: app-incidents-db

  - name: app-incidents-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: app-incidents-auth
      - name: srv-api
      - name: app-incidents-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: appincidentsincidents
          artifacts:
            - incidents.zip
          target-path: app/
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet

  - name: appincidentsincidents
    type: html5
    path: app/incidents
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: app-incidents-destinations
    type: com.sap.application.content
    requires:
      - name: app-incidents-auth
        parameters:
          service-key:
            name: app-incidents-auth-key
      - name: app-incidents-html5-repo-host
        parameters:
          service-key:
            name: app-incidents-html5-repo-host-key
      - name: srv-api
      - name: app-incidents-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: app-incidents-html5-repository
              ServiceInstanceName: app-incidents-html5-repo-host
              ServiceKeyName: app-incidents-html5-repo-host-key
              sap.cloud.service: appincidents.service
            - Name: app-incidents-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: app-incidents-auth
              ServiceKeyName: app-incidents-auth-key
              sap.cloud.service: appincidents.service

resources:
  - name: app-incidents-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: app-incidents-${org}-${space}
        tenant-mode: dedicated
        role-collections:
          - name: 'support (app-incidents ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.support'
          - name: 'admin (app-incidents ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.admin'
  - name: app-incidents-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: app-incidents-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true

  - name: app-incidents-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: app-incidents-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
